＝＝证件照生成模块＝＝

本开源项目使用BSD开源协议。

这是一个可以将任意人像照片转换为证件照的模块，使用C++11标准编写，提供C++接口。

有关如何编译和使用示例，参见about-samples。

＝＝各子目录＝＝

src － 包含模块的源代码文件和内部头文件。
include － 包含模块的接口头文件。
make － Linux、OS X 的Makefile。
vs12 － Visual Studio 2013解决方案。
